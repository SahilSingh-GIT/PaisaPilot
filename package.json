{
  "name": "paisapilot-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "PaisaPilot — AI-Powered Expense Manager & Personal Financial Insights",
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"bgYellow.bold,bgCyan.bold\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "prisma:generate": "npm run prisma:generate --prefix server",
    "prisma:studio": "npm run prisma:studio --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
